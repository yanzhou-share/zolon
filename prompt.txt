请使用 Python 的 FastAPI 构建后端，使用 Streamlit 构建前端，完成一个 MP12 智能收银终端的销售 Agent 模拟器。

需求清单：

后端 (FastAPI)：

实现 /chat 接口，接收用户消息。

使用 ChromaDB 作为向量数据库，预存 3 条关于 'MP12 智能平板' 的数据（尺寸、功能、价格）。

实现 SentimentAnalysis 函数（检测辱骂则直接返回 '转人工' 提示）。

使用 asyncio 实现 3 秒的模拟延迟，模拟 API 处理时间。

前端 (Streamlit)：

构建一个聊天界面，左侧显示 AI 头像，右侧显示用户。

包含输入框，实时显示对话历史。

逻辑要求：

引入 '上下文合并'：如果用户在 5 秒内连续发送两条消息，后端需将它们合并为一个 Prompt 再处理。

格式：输出 JSON { 'reply': str, 'status': str (normal/human) }。
